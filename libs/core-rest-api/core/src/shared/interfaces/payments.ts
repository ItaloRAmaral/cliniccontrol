export enum PaymentMethod {
  CREDIT_CARD,
  DEBIT_CARD,
  PIX,
  MONEY,
  HEALTH_INSURANCE,
  OTHER,
}

export enum Role {
  ADMIN,
  PSYCHOLOGIST,
  PATIENT,
}

export enum Plan {
  FREE,
  BASIC,
  PREMIUM,
}
